language: java
dist: trusty
sudo: false
script: "mvn -B -s travis-settings.xml -P DBs-Bundled,MySQLTest test "
services:
  - mysql
  - postgresql
jdk:
  - oraclejdk7
notifications:
  email: false
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS dbvtest;'
  - mysql -V
  - psql -c 'create database dbvtest;' -U postgres
  - psql --version
before_script: